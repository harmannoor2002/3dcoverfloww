# 3dcoverfloww

---

### 🌍 5. Push to GitHub

Once all files are ready:

```bash
git init
git add .
git commit -m "Initial commit - Dockerized 3D Coverflow website"
git branch -M main
git remote add origin https://github.com/<your-username>/<repo-name>.git
git push -u origin main
